{
  "name": "llm-logger",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "sdk",
    "ingestion",
    "chatbot",
    "frontend"
  ],
  "scripts": {
    "up": "docker compose up --build",
    "down": "docker compose down -v",
    "dev:ingestion": "npm --workspace ingestion run dev",
    "dev:chatbot": "npm --workspace chatbot run dev",
    "dev:frontend": "npm --workspace frontend run dev"
  }
}
